# Enforced server config for CreatureManager (#25): cloning and customisation stay off. The four
# data files beside this overlay (creatures/attacks/ai/projectile yml) pin the verified-empty
# state, so a future package default that ships content - the way BossRules shipped modified
# powers - is caught instead of silently loaded. Karma and levels stay mod-default: Karma is the
# reason this mod is in the stack.

[1 - General]
Lock Configuration = On
