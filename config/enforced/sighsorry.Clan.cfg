# Enforced server config for Clan (#25, #68). Applied to the generated BepInEx config by
# scripts/apply-enforced-config.sh; docs/modstack.md is the source of truth for the intent.
# Only deliberate deviations belong here - everything else stays exactly as the mod generated it.
#
# Friendly fire off inside a clan, and clan positions shared. Those two together are what a clan
# is worth: you cannot hit your own people by accident, and you can see where they are. Position
# sharing outside the clan is a separate decision and is off - PvPBiomeDominions hides every
# biome's map position (#68).

[1 - General]
Lock Configuration = On
Clan Friendly Fire = Off
Share Clan Positions = On
