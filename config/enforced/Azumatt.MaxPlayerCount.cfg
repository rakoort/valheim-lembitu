# Enforced server config for MaxPlayerCount (#68). Twenty is the Roster's capacity decision
# (CONTEXT.md, Roster; ADR-0007), and this key is where it is actually implemented - the launch
# environment carries no player-cap variable.
#
# This is the project's one remaining fork, built because no upstream release works on the game
# the run is frozen at (CONTEXT.md, Fork). The fork's own default is already twenty; it is pinned
# because a capacity is a decision, and because the value must survive a rebuild of the fork.

[1 - General]
MaxPlayerCount = 20
