# Enforced server config for InventorySlots (#25). Keep-on-death is off so PvPBiomeDominions is
# the only authority on what a death takes (ADR-0004, ticket #8).

[1 - General]
Lock Configuration = On
Enable Death Keep Rules = Off
