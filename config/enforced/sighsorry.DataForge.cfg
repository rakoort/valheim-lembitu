# Enforced server config for DataForge (#25): tuning only. When something is deliberately tuned,
# the tuned yml is committed here so the tuning is reproducible from the repo; until then the
# three data files beside this overlay pin the verified-empty state, and any content a future
# package default ships is a review, not a silent change.

[1 - General]
Lock Configuration = On
