# Enforced server config for PortalRules (#25): access modes only. No fares (Travel Cost Scope
# already defaults to Off), no portal map picker, no per-portal GlobalKey gates (none configured;
# the mod logs that gating falls back to shared world progression).

[1 - General]
Lock Configuration = On

[2 - Portal Map]
Enable Portal Map = Off
